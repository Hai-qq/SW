services:
  postgres:
    image: postgres:16
    container_name: sw-postgres
    ports:
      - '5434:5432'
    environment:
      POSTGRES_USER: sw
      POSTGRES_PASSWORD: sw
      POSTGRES_DB: sw
    volumes:
      - sw_postgres_data:/var/lib/postgresql/data

volumes:
  sw_postgres_data:
