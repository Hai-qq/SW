<!--pages/home/home.wxml-->
<view class="container" style="min-height: {{windowHeight}}px;">
  <!-- 最底层背景: 极简奶油色渐变 -->
  <view class="bg-gradient"></view>

  <!-- 顶部导航区 -->
  <view class="header" style="padding-top: {{statusBarHeight}}px;">
    <view class="header-content">
      <view class="registered-users">
        <view class="online-dot"></view>
        <text>32,419 位同频者已加入</text>
      </view>
    </view>
  </view>

  <scroll-view scroll-y="true" enable-flex class="main-content">
    
    <!-- 顶部横向好友墙/同频人列表 -->
    <scroll-view scroll-x="true" class="stories-list" show-scrollbar="false">
      <!-- 个人入口 -->
      <view class="story-item">
        <view class="avatar-wrap you-wrap">
          <view class="avatar-img avatar-placeholder"></view>
          <view class="badge black-badge"></view>
        </view>
        <text class="name-text">你</text>
      </view>
      <!-- 推荐匹配者 -->
      <view class="story-item" wx:for="{{recommendUsers}}" wx:key="id">
        <view class="avatar-wrap">
          <image wx:if="{{item.hasAvatar}}" class="avatar-img" src="{{item.avatar}}" mode="aspectFill"></image>
          <view wx:if="{{!item.hasAvatar}}" class="avatar-img avatar-placeholder"></view>
          <view class="badge"></view>
        </view>
        <text class="name-text">{{item.name}}</text>
      </view>
    </scroll-view>

    <!-- 核心卡片容器 -->
    <view class="card-wrapper">
      <view class="card-stack-layer back-one"></view>
      <view class="card-stack-layer back-two"></view>
      <view class="card-preview" style="transform: translateY(30rpx) scale({{previewScale}});">
        <view class="card-preview-body">
          <text class="card-preview-text">"{{nextCard.content}}"</text>
        </view>
      </view>
      <view 
        class="card-inner" 
        style="transform: translateX({{cardTranslateX}}px) translateY({{cardTranslateY}}px) rotate({{cardRotate}}deg); transition: {{cardTransition}};"
        catchtouchstart="touchStart"
        catchtouchmove="touchMove"
        catchtouchend="touchEnd">
        
        <!-- 卡片顶部: 标签 + 分享按钮 -->
        <view class="card-top-row">
          <view class="card-tags">
            <view class="card-tag" wx:for="{{currentCard.tags}}" wx:key="index">
              <text class="card-tag-text">{{item}}</text>
            </view>
          </view>
          <view class="share-trigger" bindtap="handleShare">
            <text class="share-trigger-icon">↗</text>
          </view>
        </view>

        <!-- 卡片正文: 观点文字 -->
        <view class="card-body">
          <text class="opinion-text">"{{currentCard.content}}"</text>
        </view>

        <!-- 卡片底部: 同频数据条 -->
        <view class="card-footer">
          <view class="vote-labels">
            <text class="label-left">A</text>
            <text class="label-right">B</text>
          </view>
          
          <view class="progress-bar-container">
            <view class="progress-bar-bg">
              <view class="progress-bar-fill" style="width: {{currentCard.agreePercent}}%;"></view>
            </view>
            <view class="avatars-agree" style="left: 0;">
              <block wx:if="{{currentCard.hasAgreeAvatars}}">
                <image wx:for="{{currentCard.agreeAvatars}}" wx:key="index" src="{{item}}" class="tiny-avatar"></image>
              </block>
            </view>
            <view class="avatars-disagree" style="right: 0;">
              <image wx:if="{{currentCard.hasDisagreeAvatar}}" src="{{currentCard.disagreeAvatar}}" class="tiny-avatar single"></image>
            </view>
          </view>

          <view class="card-actions">
            <view class="card-action-pill" bindtap="openCommentPanel">
              <view class="card-action-icon-wrap">
                <image src="/assets/icons/forum.svg" class="card-action-icon" mode="aspectFit"></image>
            </view>
              <text class="card-action-text">评论 {{currentCard.commentCount}}</text>
            </view>

            <view class="card-action-pill" bindtap="reduceSimilarTopics">
              <view class="card-action-icon-wrap">
                <image src="/assets/icons/close.svg" class="card-action-icon small" mode="aspectFit"></image>
              </view>
              <text class="card-action-text">减少此类推荐</text>
            </view>
          </view>

        </view>
      </view>
    </view>

    <!-- 底部划动动作按钮 -->
    <view class="action-buttons-wrapper">
      <!-- 评论入口 -->
      <view class="action-col" bindtap="openCommentPanel">
        <view class="circle-btn">
          <image src="/assets/icons/forum.svg" style="width: 34rpx; height: 34rpx; opacity: 0.72;"></image>
        </view>
        <view class="action-text-wrapper action-label">
          <text class="action-hint">COMMENT</text>
          <text class="action-main">评论</text>
        </view>
      </view>
      
      <!-- 中部 AI 通话 -->
      <view class="center-actions">
        <view class="voice-btn" bindtap="handleVoiceInput">
          <image src="/assets/icons/mic.svg" style="width: 34rpx; height: 34rpx; opacity: 0.8; filter: brightness(0.1);"></image>
        </view>
        <view class="action-text-wrapper action-label center-text">
          <text class="action-hint">AI CALL</text>
          <text class="action-main">连线智能</text>
        </view>
      </view>

      <!-- 降低同类话题 -->
      <view class="action-col" bindtap="reduceSimilarTopics">
        <view class="circle-btn">
          <image src="/assets/icons/close.svg" style="width: 30rpx; height: 30rpx; opacity: 0.72;"></image>
        </view>
        <view class="action-text-wrapper action-label">
          <text class="action-hint">FILTER</text>
          <text class="action-main">减少同类</text>
          <text class="action-sub">话题</text>
        </view>
      </view>
    </view>
    
  </scroll-view>

  <!-- 底部毛玻璃导航条 (三段式) -->
  <view class="bottom-nav">
    <view class="nav-container">
      <view class="nav-glass"></view>
      <view class="nav-content">
        <view class="nav-item" bindtap="goToDiscovery">
          <image src="/assets/icons/wave.svg" class="nav-svg-icon" mode="aspectFit"></image>
          <text class="nav-text">冲浪</text>
        </view>
        <view class="nav-item active">
          <image src="/assets/icons/cards_stack.svg" class="nav-svg-icon active-icon" mode="aspectFit"></image>
          <text class="nav-text">真心话</text>
        </view>
        <view class="nav-item" bindtap="goToChat">
          <image src="/assets/icons/note.svg" class="nav-svg-icon" mode="aspectFit"></image>
          <text class="nav-text">小纸条</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 上滑次数耗尽弹窗 -->
  <view class="modal-mask" wx:if="{{showLimitModal}}" catchtouchmove="preventTouchMove">
    <view class="modal-box">
      <text class="modal-title">次数用尽</text>
      <text class="modal-desc">今日基础用户上滑划过功能 (5次) 已用尽，升级三观库解锁无限划卡功能。</text>
      <button class="modal-btn-primary" bindtap="closeModal">确认</button>
    </view>
  </view>

  <view class="comment-mask" wx:if="{{showCommentPanel}}" catchtouchmove="preventTouchMove">
    <view class="comment-panel">
      <view class="comment-panel-header">
        <text class="comment-panel-title">卡片评论</text>
        <text class="comment-panel-close" bindtap="closeCommentPanel">关闭</text>
      </view>
      <text class="comment-card-text">"{{currentCard.content}}"</text>
      <view wx:if="{{commentsEmpty}}" class="comment-empty">还没有评论，写下第一句共鸣。</view>
      <scroll-view scroll-y="true" class="comment-list">
        <view wx:for="{{comments}}" wx:key="commentId" class="comment-row">
          <text class="comment-author">{{item.author.nickname}}</text>
          <text class="comment-content">{{item.content}}</text>
        </view>
      </scroll-view>
      <textarea
        class="comment-input"
        value="{{commentInput}}"
        bindinput="onCommentInput"
        maxlength="300"
        placeholder="写一句你的看法"
      ></textarea>
      <button class="comment-submit" bindtap="submitComment">发送评论</button>
    </view>
  </view>

  <view class="share-mask" wx:if="{{showSharePanel}}" catchtouchmove="preventTouchMove" catchtap="closeSharePanel">
    <view class="share-panel" catchtap="noop">
      <text class="share-title">分享观点卡片</text>
      <text class="share-desc">生成话题海报后，可直接分享给微信好友或朋友圈。</text>

      <view class="share-preview">
        <text class="share-preview-tag">{{(currentCard.tags && currentCard.tags[0]) || '全部'}}</text>
        <text class="share-preview-text">"{{currentCard.content}}"</text>
      </view>

      <view class="share-actions">
        <button class="share-btn ghost" bindtap="generateSharePoster" loading="{{sharePosterGenerating}}">生成海报</button>
        <button class="share-btn solid" bindtap="shareToWechatFriend">微信好友</button>
        <button class="share-btn solid" bindtap="shareToTimeline">朋友圈</button>
        <button class="share-btn ghost" bindtap="savePosterToAlbum">保存到相册</button>
      </view>

      <button class="share-close" bindtap="closeSharePanel">取消</button>
    </view>
  </view>

  <canvas canvas-id="sharePosterCanvas" class="poster-canvas"></canvas>

</view>
